package(default_visibility=["//visibility:public"])

load("@npm//@bazel/rollup:index.bzl", "rollup_bundle")
load("@npm//@bazel/typescript:index.bzl", "ts_library")

ts_library(
    name="lib",
    srcs=glob(["**/*.ts"]),
    deps=[
        "//core:lib",
        "//frontend:lib",
    ],
)

rollup_bundle(
    name="bundle",
    entry_points={"//context/browser:index.ts": "browser"},
    deps=[
        "//context/browser:lib",
    ],
)
