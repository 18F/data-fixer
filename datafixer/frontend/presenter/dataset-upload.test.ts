import { DatasetUploadPresenter } from './dataset-upload';

describe('dataset upload presenter', () => {
  it('should reload page after upload', () => {
    const presenter = DatasetUploadPresenter();
  });
});
