import { SessionPresenter } from './session';

describe('session presenter', () => {
  beforeEach(() => {
    //SessionPresenter();
  });

  it('should work', () => {
    expect(true).toBe(true);
  });
});
